apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: identity-subsystem-ms1
spec:
  releaseName: identity-subsystem-ms1
  chart:
    spec:
      chart: podinfo
      version: "null"
      sourceRef:
        kind: HelmRepository
        name: ecr
        namespace: flux-system
  interval: 5m

